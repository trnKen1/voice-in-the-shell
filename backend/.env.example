# Metered API key (NOT a claude.ai / Claude Code subscription login —
# third-party apps built on the Claude Agent SDK can't use subscription auth).
# Get one at https://platform.claude.com/
ANTHROPIC_API_KEY=
